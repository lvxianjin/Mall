<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qdlg.dao.UserDao">
    <resultMap type="com.qdlg.model.User" id="UserResult">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="account" jdbcType="VARCHAR" property="account"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="phone" jdbcType="VARCHAR" property="phone"/>
        <result column="nick_name" jdbcType="VARCHAR" property="nick_name"/>
    </resultMap>
    <select id="checkUser" parameterType="java.util.Map" resultMap="java.util.Map">
        select account,password from user
        <where>
            account=#{account} and password=#{password}
        </where>
    </select>
</mapper>
